{
  "name": "@oway/sdk-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Official Oway SDKs for multiple languages",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "generate": "node scripts/generate.js",
    "generate:local": "node scripts/generate.js",
    "generate:sandbox": "node scripts/generate.js --sandbox",
    "generate:production": "node scripts/generate.js --production",
    "generate:skip-fetch": "node scripts/generate.js --skip-fetch",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Oway-Inc/oway-sdk.git"
  },
  "keywords": [
    "oway",
    "sdk",
    "shipping",
    "logistics",
    "freight"
  ],
  "author": "Oway",
  "license": "MIT"
}
